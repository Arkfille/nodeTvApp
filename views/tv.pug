html(ng-app="tvApp")
  head
    title
      NodeTV
    script(src="https://apis.google.com/js/api.js")      
    link(href="https://fonts.googleapis.com/css?family=Lato" rel="stylesheet")
    link(rel='stylesheet', href='/stylesheets/tv.css')
    script(src='https://cdn.socket.io/socket.io-1.4.5.js')
    script(src="https://ajax.googleapis.com/ajax/libs/angularjs/1.5.7/angular.min.js")
    script(src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js")
    script(src="//cdnjs.cloudflare.com/ajax/libs/annyang/2.6.0/annyang.min.js")
    script(src="//cdnjs.cloudflare.com/ajax/libs/annyang/2.6.0/annyang.min.js")
    script(src="//cdnjs.cloudflare.com/ajax/libs/SpeechKITT/0.3.0/speechkitt.min.js")  
    script.
      var ytApi = !{JSON.stringify(data)};
      console.log(ytApi);
    script(src='/javascripts/helpers.js')
    script(src='/javascripts/tvClient.js')
    link(rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons")
    link(rel="stylesheet" href="https://code.getmdl.io/1.3.0/material.indigo-pink.min.css")
    script(defer src="https://code.getmdl.io/1.3.0/material.min.js")
    
    
  body
    block content

      div.wrapper
        div.darken
        div#logo
          img(src="../images/nodeTV-logo.gif" alt="node-TV") 
        div.nav
          div.navTabs
            div(class="selected" id="home")
              p Home
            div( id="videos")
              p Videos
            div#kategories 
              p Kategories
            div( id="settings") 
              p Settings
        div.content
          //- p#resp= ytapi
          div.contentPage.home.pageActive(ng-controller="videoStream")
            div#rand
              div(id="featuredVideo"  class="thumbnail")
                h2(id="searchTitle") Welcome to nodeTv!
                h4(id="helpText") In nodeTv you can use voice commands to control most inputs <br> try saying "search G-eazy" to get a list of G-eazy videos or try "play first video" to play the first video in the list below <br> or why not try both combined?
              div.homeVideos
                div.videoCard(ng-repeat="video in videos")
                  div.hoverWrap(id="videoStream-{{$index + 1}}" data-watchid="{{video.id.videoId}}")
                    <img class="thumbnail" src={{video.snippet.thumbnails.high.url}} />
                    div.shadow
                      p {{video.snippet.title}}
                   
          div.contentPage.videos
            h3 Videos
            
          div.contentPage.settings
            h3 This TV's id is: <span id="roomID">null</span>
      
      div.videoPlayer
        <div id="player"></div>
      div#footer
